<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NN.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NN"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top">
        <TextBlock Margin="10,5,0,10" Text="Settings" />
        <StackPanel Margin="10,5,0,10"  Orientation="Horizontal">
            <TextBlock Margin="10,10,0,10" Text="{x:Bind txt(21);}" />
            <TextBox x:Name="PythonLocation1" Margin="10,5,0,10"  Text="{x:Bind PythonLocation,Mode=TwoWay}" />
            <Button Content="..." Margin="10,5,0,10" Click="OnPythonLocation" />    
        </StackPanel>
    </StackPanel>
</Page>
